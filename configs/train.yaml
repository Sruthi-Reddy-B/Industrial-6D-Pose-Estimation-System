dataset:
  hdf5_path: data/processed/dataset.h5
  batch_size: 8
  num_workers: 0  # Use 0 for single process, 4-8 for production
  image_size: [480, 640]

training:
  epochs: 10
  lr: 0.001
  save_path: experiments/exp_001
